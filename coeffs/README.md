# Coefficients

This folder simply contains the Chebyshev coefficients for the eight polynomials used to decompose a number from $[0, 225] \subset \mathbb{R}$ to binary representation, as explained in the paper.

We store them here for efficiency reasons, so we do not have to compute them every run.
